#ifndef JM_LEVEL_ONE_H
#define JM_LEVEL_ONE_H

#include <stdbool.h>

struct level_one_t {
  // TODO create character struct and add
  // TODO create score component and add
};

bool level_one_init(struct level_one_t* level);
void level_one_free(struct level_one_t* level);

#endif
